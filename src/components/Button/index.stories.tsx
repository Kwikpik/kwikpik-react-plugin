import React from "react";
import { Meta, StoryObj } from "@storybook/react";
import Button from ".";

const meta: Meta<typeof Button> = {
  component: Button,
  title: "/core/components/CustomButton",
};

export default meta;
